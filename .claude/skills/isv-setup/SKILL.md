---
name: isv-setup
description: >
  Guided wizard that walks an ISV through setting up CI for their UDS package.
  Asks platform, runs setup:generate, walks them through filling in every
  REPLACE_ME value interactively, then runs setup:validate. Use when an ISV
  asks "how do I set up CI", "get me started with CI", "configure my pipeline",
  or "what do I do after setup:generate". Triggers on: "set up CI", "configure
  CI", "onboard CI", "isv setup", "setup:generate", "get started with UDM CI".
---

Read and follow `.claude/skills/isv-setup/RUNBOOK.md` exactly.
